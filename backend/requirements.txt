fastapi==0.137.1
uvicorn[standard]==0.32.1
sqlalchemy==2.0.50
pydantic-settings==2.6.1
python-jose[cryptography]==3.5.0
bcrypt==4.2.1
docker==7.1.0
httpx==0.27.2
python-multipart==0.0.32
# Composites the Cove watermark onto project logos to bake per-image PWA icons
# (server.icons). Self-contained manylinux wheels — no system libs needed.
Pillow==12.3.0
# Prebuilt SQLCipher wheels are published for x86_64 only. On other arches
# (e.g. arm64) there is no wheel, so scope this to x86_64 — the app imports
# sqlcipher3 lazily and only when COVE_DB_ENCRYPTION_KEY is set (see db.py).
# To use DB encryption on arm64, build sqlcipher3 from source instead.
sqlcipher3-binary==0.5.4; platform_machine == "x86_64"
pytest==9.0.3
pytest-asyncio==0.24.0
